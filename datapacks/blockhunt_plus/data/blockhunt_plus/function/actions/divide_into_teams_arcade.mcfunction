execute \
    as @r[team=!hiders, team=!seekers] run \
        team join hiders

execute \
    as @r[team=!hiders, team=!seekers] run \
        team join hiders

execute \
    as @r[team=!hiders, team=!seekers] run \
        team join hiders

execute \
    as @r[team=!hiders, team=!seekers] run \
        team join hiders

execute \
    as @r[team=!hiders, team=!seekers] run \
        team join seekers

execute \
    if entity @a[team=!hiders, team=!seekers] run \
        function blockhunt_plus:actions/divide_into_teams_arcade